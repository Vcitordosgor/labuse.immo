---
import Reveal from './Reveal.astro';

// ⚠️ Placeholders NEUTRES uniquement — ce ne sont pas de vrais clients.
// TODO: remplacer par vrais logos clients avec accord (voir README).
const placeholders = ['◆ TERRA', '▲ LITTORAL', '● HORIZON', '■ BÂTIR', '⬢ MASCARIN'];
---

<section class="border-t border-white/5">
  <div class="mx-auto max-w-content px-5 py-14 sm:px-8">
    <Reveal>
      <div
        class="flex flex-col items-start gap-8 rounded-3xl border border-coast/10 bg-surface/50 px-6 py-8 sm:px-10 md:flex-row md:items-center md:justify-between"
      >
        <p class="flex-none text-[16px] text-[#aab5b0] sm:text-[18px]">
          Déjà adopté par des acteurs qui construisent La Réunion.
        </p>
        <!-- TODO: remplacer par vrais logos clients avec accord -->
        <ul class="flex flex-wrap items-center gap-x-9 gap-y-4" aria-label="Partenaires (exemples)">
          {
            placeholders.map((mark) => (
              <li class="select-none text-[19px] font-bold tracking-[.04em] text-[#5f6a64] sm:text-[22px]">
                {mark}
              </li>
            ))
          }
        </ul>
      </div>
      <p class="mt-3 px-2 text-[12px] text-muted/70">
        Logos illustratifs — remplacés par de vrais clients dès accord signé.
      </p>
    </Reveal>
  </div>
</section>
